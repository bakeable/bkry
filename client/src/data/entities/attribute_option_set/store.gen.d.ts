import { type AttributeOptionSet } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the attributeOptionSet store.
 */
export interface AttributeOptionSetStoreState {
  /**
   * The currently active attributeOptionSet ID
   */
  activeID: string | null

  /**
   * A cache of attributeOptionSets.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of attributeOptionSets.
   */
  list: AttributeOptionSet[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an AttributeOptionSetStore that extends the Store interface.
 */
export interface AttributeOptionSetStore extends AttributeOptionSetStoreState, Store {
  /**
   * Fire a form update event for the AttributeOptionSet.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all AttributeOptionSet.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active attributeOptionSet
   */
  active: AttributeOptionSet

  /**
   * Adds an attributeOptionSet to the local AttributeOptionSetStore.
   * @param attributeOptionSet The attributeOptionSet to be added.
   */
  add: (attributeOptionSet: AttributeOptionSet) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an attributeOptionSet from the AttributeOptionSetStore from the external database.
   * @param attributeOptionSet The attributeOptionSet to be deleted.
   * @returns {Promise<void>} A promise that resolves when the attributeOptionSet is deleted.
   */
  delete: (attributeOptionSet: AttributeOptionSet) => Promise<void>

  /**
   * Deletes an attributeOptionSet by ID from the AttributeOptionSetStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the attributeOptionSet is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a AttributeOptionSet from the local AttributeOptionSetStore.
   * @returns { AttributeOptionSet | null } The attributeOptionSet.
   */
  get: (id: string) => AttributeOptionSet | null

  /**
   * Reinserts an attributeOptionSet to the AttributeOptionSetStore list.
   * @param attributeOptionSet The attributeOptionSet to be reinserted.
   * @returns {void}
   */
  reinsert: (attributeOptionSet: AttributeOptionSet) => void

  /**
   * Removes an attributeOptionSet from the local AttributeOptionSetStore.
   * @param attributeOptionSet The attributeOptionSet to be removed.
   * @returns {void}
   */
  remove: (attributeOptionSet: AttributeOptionSet) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single AttributeOptionSet from either the local AttributeOptionSetStore or external source, if forced or required.
    * @param id The ID of the AttributeOptionSet to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the attributeOptionSet is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<AttributeOptionSet>

  /**
   * Retrieves all attributeOptionSets from the AttributeOptionSetStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of attributeOptionSets.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: AttributeOptionSet[]
  }>

  /**
    * Retrieves attributeOptionSets paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of attributeOptionSets.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: AttributeOptionSet[]
  }>

  /**
    * Queries attributeOptionSets from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of attributeOptionSets.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: AttributeOptionSet[]
  }>

  /**
   * Update an attributeOptionSet from the AttributeOptionSetStore in the external database.
   * @param attributeOptionSet The attributeOptionSet to be updated.
   * @param connect Indicates whether the attributeOptionSet should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the attributeOptionSet is updated.
   */
  update: (attributeOptionSet: AttributeOptionSet) => Promise<string>

  /**
    * Searches for attributeOptionSets in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of attributeOptionSets.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<AttributeOptionSet[]>
}
