import { type Attribute } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the attribute store.
 */
export interface AttributeStoreState {
  /**
   * The currently active attribute ID
   */
  activeID: string | null

  /**
   * A cache of attributes.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of attributes.
   */
  list: Attribute[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an AttributeStore that extends the Store interface.
 */
export interface AttributeStore extends AttributeStoreState, Store {
  /**
   * Fire a form update event for the Attribute.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all Attribute.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active attribute
   */
  active: Attribute

  /**
   * Adds an attribute to the local AttributeStore.
   * @param attribute The attribute to be added.
   */
  add: (attribute: Attribute) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an attribute from the AttributeStore from the external database.
   * @param attribute The attribute to be deleted.
   * @returns {Promise<void>} A promise that resolves when the attribute is deleted.
   */
  delete: (attribute: Attribute) => Promise<void>

  /**
   * Deletes an attribute by ID from the AttributeStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the attribute is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a Attribute from the local AttributeStore.
   * @returns { Attribute | null } The attribute.
   */
  get: (id: string) => Attribute | null

  /**
   * Reinserts an attribute to the AttributeStore list.
   * @param attribute The attribute to be reinserted.
   * @returns {void}
   */
  reinsert: (attribute: Attribute) => void

  /**
   * Removes an attribute from the local AttributeStore.
   * @param attribute The attribute to be removed.
   * @returns {void}
   */
  remove: (attribute: Attribute) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single Attribute from either the local AttributeStore or external source, if forced or required.
    * @param id The ID of the Attribute to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the attribute is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<Attribute>

  /**
   * Retrieves all attributes from the AttributeStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of attributes.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: Attribute[]
  }>

  /**
    * Retrieves attributes paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of attributes.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Attribute[]
  }>

  /**
    * Queries attributes from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of attributes.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Attribute[]
  }>

  /**
   * Update an attribute from the AttributeStore in the external database.
   * @param attribute The attribute to be updated.
   * @param connect Indicates whether the attribute should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the attribute is updated.
   */
  update: (attribute: Attribute) => Promise<string>

  /**
    * Searches for attributes in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of attributes.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<Attribute[]>
}
