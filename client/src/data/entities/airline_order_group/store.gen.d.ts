import { type AirlineOrderGroup } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the airlineOrderGroup store.
 */
export interface AirlineOrderGroupStoreState {
  /**
   * The currently active airlineOrderGroup ID
   */
  activeID: string | null

  /**
   * A cache of airlineOrderGroups.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of airlineOrderGroups.
   */
  list: AirlineOrderGroup[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an AirlineOrderGroupStore that extends the Store interface.
 */
export interface AirlineOrderGroupStore extends AirlineOrderGroupStoreState, Store {
  /**
   * Fire a form update event for the AirlineOrderGroup.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all AirlineOrderGroup.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active airlineOrderGroup
   */
  active: AirlineOrderGroup

  /**
   * Adds an airlineOrderGroup to the local AirlineOrderGroupStore.
   * @param airlineOrderGroup The airlineOrderGroup to be added.
   */
  add: (airlineOrderGroup: AirlineOrderGroup) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an airlineOrderGroup from the AirlineOrderGroupStore from the external database.
   * @param airlineOrderGroup The airlineOrderGroup to be deleted.
   * @returns {Promise<void>} A promise that resolves when the airlineOrderGroup is deleted.
   */
  delete: (airlineOrderGroup: AirlineOrderGroup) => Promise<void>

  /**
   * Deletes an airlineOrderGroup by ID from the AirlineOrderGroupStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the airlineOrderGroup is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a AirlineOrderGroup from the local AirlineOrderGroupStore.
   * @returns { AirlineOrderGroup | null } The airlineOrderGroup.
   */
  get: (id: string) => AirlineOrderGroup | null

  /**
   * Reinserts an airlineOrderGroup to the AirlineOrderGroupStore list.
   * @param airlineOrderGroup The airlineOrderGroup to be reinserted.
   * @returns {void}
   */
  reinsert: (airlineOrderGroup: AirlineOrderGroup) => void

  /**
   * Removes an airlineOrderGroup from the local AirlineOrderGroupStore.
   * @param airlineOrderGroup The airlineOrderGroup to be removed.
   * @returns {void}
   */
  remove: (airlineOrderGroup: AirlineOrderGroup) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single AirlineOrderGroup from either the local AirlineOrderGroupStore or external source, if forced or required.
    * @param id The ID of the AirlineOrderGroup to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the airlineOrderGroup is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<AirlineOrderGroup>

  /**
   * Retrieves all airlineOrderGroups from the AirlineOrderGroupStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of airlineOrderGroups.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: AirlineOrderGroup[]
  }>

  /**
    * Retrieves airlineOrderGroups paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of airlineOrderGroups.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: AirlineOrderGroup[]
  }>

  /**
    * Queries airlineOrderGroups from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of airlineOrderGroups.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: AirlineOrderGroup[]
  }>

  /**
   * Update an airlineOrderGroup from the AirlineOrderGroupStore in the external database.
   * @param airlineOrderGroup The airlineOrderGroup to be updated.
   * @param connect Indicates whether the airlineOrderGroup should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the airlineOrderGroup is updated.
   */
  update: (airlineOrderGroup: AirlineOrderGroup) => Promise<string>

  /**
    * Searches for airlineOrderGroups in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of airlineOrderGroups.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<AirlineOrderGroup[]>
}
