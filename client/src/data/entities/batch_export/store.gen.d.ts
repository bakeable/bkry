import { type BatchExport } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the batchExport store.
 */
export interface BatchExportStoreState {
  /**
   * The currently active batchExport ID
   */
  activeID: string | null

  /**
   * A cache of batchExports.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of batchExports.
   */
  list: BatchExport[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an BatchExportStore that extends the Store interface.
 */
export interface BatchExportStore extends BatchExportStoreState, Store {
  /**
   * Fire a form update event for the BatchExport.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all BatchExport.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active batchExport
   */
  active: BatchExport

  /**
   * Adds an batchExport to the local BatchExportStore.
   * @param batchExport The batchExport to be added.
   */
  add: (batchExport: BatchExport) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an batchExport from the BatchExportStore from the external database.
   * @param batchExport The batchExport to be deleted.
   * @returns {Promise<void>} A promise that resolves when the batchExport is deleted.
   */
  delete: (batchExport: BatchExport) => Promise<void>

  /**
   * Deletes an batchExport by ID from the BatchExportStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the batchExport is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a BatchExport from the local BatchExportStore.
   * @returns { BatchExport | null } The batchExport.
   */
  get: (id: string) => BatchExport | null

  /**
   * Reinserts an batchExport to the BatchExportStore list.
   * @param batchExport The batchExport to be reinserted.
   * @returns {void}
   */
  reinsert: (batchExport: BatchExport) => void

  /**
   * Removes an batchExport from the local BatchExportStore.
   * @param batchExport The batchExport to be removed.
   * @returns {void}
   */
  remove: (batchExport: BatchExport) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single BatchExport from either the local BatchExportStore or external source, if forced or required.
    * @param id The ID of the BatchExport to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the batchExport is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<BatchExport>

  /**
   * Retrieves all batchExports from the BatchExportStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of batchExports.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: BatchExport[]
  }>

  /**
    * Retrieves batchExports paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of batchExports.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: BatchExport[]
  }>

  /**
    * Queries batchExports from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of batchExports.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: BatchExport[]
  }>

  /**
   * Update an batchExport from the BatchExportStore in the external database.
   * @param batchExport The batchExport to be updated.
   * @param connect Indicates whether the batchExport should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the batchExport is updated.
   */
  update: (batchExport: BatchExport) => Promise<string>

  /**
    * Searches for batchExports in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of batchExports.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<BatchExport[]>
}
