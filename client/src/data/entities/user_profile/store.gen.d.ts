import { type UserProfile } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the userProfile store.
 */
export interface UserProfileStoreState {
  /**
   * The currently active userProfile ID
   */
  activeID: string | null

  /**
   * A cache of userProfiles.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of userProfiles.
   */
  list: UserProfile[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an UserProfileStore that extends the Store interface.
 */
export interface UserProfileStore extends UserProfileStoreState, Store {
  /**
   * Fire a form update event for the UserProfile.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all UserProfile.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active userProfile
   */
  active: UserProfile

  /**
   * Adds an userProfile to the local UserProfileStore.
   * @param userProfile The userProfile to be added.
   */
  add: (userProfile: UserProfile) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an userProfile from the UserProfileStore from the external database.
   * @param userProfile The userProfile to be deleted.
   * @returns {Promise<void>} A promise that resolves when the userProfile is deleted.
   */
  delete: (userProfile: UserProfile) => Promise<void>

  /**
   * Deletes an userProfile by ID from the UserProfileStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the userProfile is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a UserProfile from the local UserProfileStore.
   * @returns { UserProfile | null } The userProfile.
   */
  get: (id: string) => UserProfile | null

  /**
   * Reinserts an userProfile to the UserProfileStore list.
   * @param userProfile The userProfile to be reinserted.
   * @returns {void}
   */
  reinsert: (userProfile: UserProfile) => void

  /**
   * Removes an userProfile from the local UserProfileStore.
   * @param userProfile The userProfile to be removed.
   * @returns {void}
   */
  remove: (userProfile: UserProfile) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single UserProfile from either the local UserProfileStore or external source, if forced or required.
    * @param id The ID of the UserProfile to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the userProfile is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<UserProfile>

  /**
   * Retrieves all userProfiles from the UserProfileStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of userProfiles.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: UserProfile[]
  }>

  /**
    * Retrieves userProfiles paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of userProfiles.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: UserProfile[]
  }>

  /**
    * Queries userProfiles from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of userProfiles.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: UserProfile[]
  }>

  /**
   * Update an userProfile from the UserProfileStore in the external database.
   * @param userProfile The userProfile to be updated.
   * @param connect Indicates whether the userProfile should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the userProfile is updated.
   */
  update: (userProfile: UserProfile) => Promise<string>

  /**
    * Searches for userProfiles in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of userProfiles.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<UserProfile[]>
}
