import { type ExaminationProperty } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the examinationProperty store.
 */
export interface ExaminationPropertyStoreState {
  /**
   * The currently active examinationProperty ID
   */
  activeID: string | null

  /**
   * A cache of examinationPropertys.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of examinationPropertys.
   */
  list: ExaminationProperty[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an ExaminationPropertyStore that extends the Store interface.
 */
export interface ExaminationPropertyStore extends ExaminationPropertyStoreState, Store {
  /**
   * Fire a form update event for the ExaminationProperty.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all ExaminationProperty.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active examinationProperty
   */
  active: ExaminationProperty

  /**
   * Adds an examinationProperty to the local ExaminationPropertyStore.
   * @param examinationProperty The examinationProperty to be added.
   */
  add: (examinationProperty: ExaminationProperty) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an examinationProperty from the ExaminationPropertyStore from the external database.
   * @param examinationProperty The examinationProperty to be deleted.
   * @returns {Promise<void>} A promise that resolves when the examinationProperty is deleted.
   */
  delete: (examinationProperty: ExaminationProperty) => Promise<void>

  /**
   * Deletes an examinationProperty by ID from the ExaminationPropertyStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the examinationProperty is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a ExaminationProperty from the local ExaminationPropertyStore.
   * @returns { ExaminationProperty | null } The examinationProperty.
   */
  get: (id: string) => ExaminationProperty | null

  /**
   * Reinserts an examinationProperty to the ExaminationPropertyStore list.
   * @param examinationProperty The examinationProperty to be reinserted.
   * @returns {void}
   */
  reinsert: (examinationProperty: ExaminationProperty) => void

  /**
   * Removes an examinationProperty from the local ExaminationPropertyStore.
   * @param examinationProperty The examinationProperty to be removed.
   * @returns {void}
   */
  remove: (examinationProperty: ExaminationProperty) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single ExaminationProperty from either the local ExaminationPropertyStore or external source, if forced or required.
    * @param id The ID of the ExaminationProperty to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the examinationProperty is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<ExaminationProperty>

  /**
   * Retrieves all examinationPropertys from the ExaminationPropertyStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of examinationPropertys.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: ExaminationProperty[]
  }>

  /**
    * Retrieves examinationPropertys paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of examinationPropertys.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: ExaminationProperty[]
  }>

  /**
    * Queries examinationPropertys from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of examinationPropertys.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: ExaminationProperty[]
  }>

  /**
   * Update an examinationProperty from the ExaminationPropertyStore in the external database.
   * @param examinationProperty The examinationProperty to be updated.
   * @param connect Indicates whether the examinationProperty should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the examinationProperty is updated.
   */
  update: (examinationProperty: ExaminationProperty) => Promise<string>

  /**
    * Searches for examinationPropertys in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of examinationPropertys.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<ExaminationProperty[]>
}
