import { type PrintingOrderItem } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the printingOrderItem store.
 */
export interface PrintingOrderItemStoreState {
  /**
   * The currently active printingOrderItem ID
   */
  activeID: string | null

  /**
   * A cache of printingOrderItems.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of printingOrderItems.
   */
  list: PrintingOrderItem[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an PrintingOrderItemStore that extends the Store interface.
 */
export interface PrintingOrderItemStore extends PrintingOrderItemStoreState, Store {
  /**
   * Fire a form update event for the PrintingOrderItem.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all PrintingOrderItem.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active printingOrderItem
   */
  active: PrintingOrderItem

  /**
   * Adds an printingOrderItem to the local PrintingOrderItemStore.
   * @param printingOrderItem The printingOrderItem to be added.
   */
  add: (printingOrderItem: PrintingOrderItem) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an printingOrderItem from the PrintingOrderItemStore from the external database.
   * @param printingOrderItem The printingOrderItem to be deleted.
   * @returns {Promise<void>} A promise that resolves when the printingOrderItem is deleted.
   */
  delete: (printingOrderItem: PrintingOrderItem) => Promise<void>

  /**
   * Deletes an printingOrderItem by ID from the PrintingOrderItemStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the printingOrderItem is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a PrintingOrderItem from the local PrintingOrderItemStore.
   * @returns { PrintingOrderItem | null } The printingOrderItem.
   */
  get: (id: string) => PrintingOrderItem | null

  /**
   * Reinserts an printingOrderItem to the PrintingOrderItemStore list.
   * @param printingOrderItem The printingOrderItem to be reinserted.
   * @returns {void}
   */
  reinsert: (printingOrderItem: PrintingOrderItem) => void

  /**
   * Removes an printingOrderItem from the local PrintingOrderItemStore.
   * @param printingOrderItem The printingOrderItem to be removed.
   * @returns {void}
   */
  remove: (printingOrderItem: PrintingOrderItem) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single PrintingOrderItem from either the local PrintingOrderItemStore or external source, if forced or required.
    * @param id The ID of the PrintingOrderItem to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the printingOrderItem is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<PrintingOrderItem>

  /**
   * Retrieves all printingOrderItems from the PrintingOrderItemStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of printingOrderItems.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: PrintingOrderItem[]
  }>

  /**
    * Retrieves printingOrderItems paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of printingOrderItems.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: PrintingOrderItem[]
  }>

  /**
    * Queries printingOrderItems from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of printingOrderItems.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: PrintingOrderItem[]
  }>

  /**
   * Update an printingOrderItem from the PrintingOrderItemStore in the external database.
   * @param printingOrderItem The printingOrderItem to be updated.
   * @param connect Indicates whether the printingOrderItem should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the printingOrderItem is updated.
   */
  update: (printingOrderItem: PrintingOrderItem) => Promise<string>

  /**
    * Searches for printingOrderItems in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of printingOrderItems.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<PrintingOrderItem[]>
}
