import { type AirlineSettings } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the airlineSettings store.
 */
export interface AirlineSettingsStoreState {
  /**
   * The currently active airlineSettings ID
   */
  activeID: string | null

  /**
   * A cache of airlineSettingss.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of airlineSettingss.
   */
  list: AirlineSettings[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an AirlineSettingsStore that extends the Store interface.
 */
export interface AirlineSettingsStore extends AirlineSettingsStoreState, Store {
  /**
   * Fire a form update event for the AirlineSettings.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all AirlineSettings.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active airlineSettings
   */
  active: AirlineSettings

  /**
   * Adds an airlineSettings to the local AirlineSettingsStore.
   * @param airlineSettings The airlineSettings to be added.
   */
  add: (airlineSettings: AirlineSettings) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an airlineSettings from the AirlineSettingsStore from the external database.
   * @param airlineSettings The airlineSettings to be deleted.
   * @returns {Promise<void>} A promise that resolves when the airlineSettings is deleted.
   */
  delete: (airlineSettings: AirlineSettings) => Promise<void>

  /**
   * Deletes an airlineSettings by ID from the AirlineSettingsStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the airlineSettings is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a AirlineSettings from the local AirlineSettingsStore.
   * @returns { AirlineSettings | null } The airlineSettings.
   */
  get: (id: string) => AirlineSettings | null

  /**
   * Reinserts an airlineSettings to the AirlineSettingsStore list.
   * @param airlineSettings The airlineSettings to be reinserted.
   * @returns {void}
   */
  reinsert: (airlineSettings: AirlineSettings) => void

  /**
   * Removes an airlineSettings from the local AirlineSettingsStore.
   * @param airlineSettings The airlineSettings to be removed.
   * @returns {void}
   */
  remove: (airlineSettings: AirlineSettings) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single AirlineSettings from either the local AirlineSettingsStore or external source, if forced or required.
    * @param id The ID of the AirlineSettings to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the airlineSettings is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<AirlineSettings>

  /**
   * Retrieves all airlineSettingss from the AirlineSettingsStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of airlineSettingss.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: AirlineSettings[]
  }>

  /**
    * Retrieves airlineSettingss paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of airlineSettingss.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: AirlineSettings[]
  }>

  /**
    * Queries airlineSettingss from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of airlineSettingss.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: AirlineSettings[]
  }>

  /**
   * Update an airlineSettings from the AirlineSettingsStore in the external database.
   * @param airlineSettings The airlineSettings to be updated.
   * @param connect Indicates whether the airlineSettings should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the airlineSettings is updated.
   */
  update: (airlineSettings: AirlineSettings) => Promise<string>

  /**
    * Searches for airlineSettingss in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of airlineSettingss.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<AirlineSettings[]>
}
