import { type CategoryLocalisation } from './interface.gen'
import { type Store } from 'pinia'
import { Pagination } from "../../general/pagination"
import { Query } from "../../general/query"

/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the categoryLocalisation store.
 */
export interface CategoryLocalisationStoreState {
  /**
   * The currently active categoryLocalisation ID
   */
  activeID: string | null

  /**
   * A cache of categoryLocalisations.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of categoryLocalisations.
   */
  list: CategoryLocalisation[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an CategoryLocalisationStore that extends the Store interface.
 */
export interface CategoryLocalisationStore extends CategoryLocalisationStoreState, Store {
  /**
   * The currently active categoryLocalisation
   */
  active: CategoryLocalisation | null

  /**
   * Adds an categoryLocalisation to the local CategoryLocalisationStore.
   * @param categoryLocalisation The categoryLocalisation to be added.
   */
  add: (categoryLocalisation: CategoryLocalisation) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Creates a new categoryLocalisation in the external database and updates the store.
   * @param categoryLocalisation The categoryLocalisation to be created.
   * @returns {Promise<CategoryLocalisation>} A promise that resolves with the created categoryLocalisation.
   */
  create: (categoryId: string, categoryLocalisation: CategoryLocalisation) => Promise<CategoryLocalisation>

  /**
   * Deletes an categoryLocalisation from the CategoryLocalisationStore from the external database.
   * @param categoryLocalisation The categoryLocalisation to be deleted.
   * @returns {Promise<void>} A promise that resolves when the categoryLocalisation is deleted.
   */
  delete: (categoryLocalisation: CategoryLocalisation) => Promise<void>

  /**
   * Deletes an categoryLocalisation by ID from the CategoryLocalisationStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the categoryLocalisation is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Fire a form update event for the CategoryLocalisation.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all CategoryLocalisations.
   */
  emitStoreUpdateEvent: () => void

  /**
   * Get a CategoryLocalisation from the local CategoryLocalisationStore.
   * @returns { CategoryLocalisation | null } The categoryLocalisation.
   */
  get: (id: string) => CategoryLocalisation | null

  /**
   * Queries categoryLocalisations from the external source
   * @param queries The queries to be used for filtering.
   * @param pagination The pagination to be used for the query.
   * @param force Whether to force the query to the external source.
   * @returns {Promise<{ pagination: Pagination; items: CategoryLocalisation[] }>} A promise that resolves with queried categoryLocalisations.
   */
  query: (categoryId: string, queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: CategoryLocalisation[]
  }>

  /**
   * Reinserts an categoryLocalisation to the CategoryLocalisationStore list.
   * @param categoryLocalisation The categoryLocalisation to be reinserted.
   * @returns {void}
   */
  reinsert: (categoryLocalisation: CategoryLocalisation) => void

  /**
   * Removes an categoryLocalisation from the local CategoryLocalisationStore.
   * @param categoryLocalisation The categoryLocalisation to be removed.
   * @returns {void}
   */
  remove: ({ id }: CategoryLocalisation) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single CategoryLocalisation from either the local CategoryLocalisationStore or external source, if forced or required.
   * @param id The ID of the CategoryLocalisation to retrieve.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<CategoryLocalisation>} A promise that resolves when the categoryLocalisation is set as active.
   */
  retrieve: (categoryId: string, id: string, force?: boolean) => Promise<CategoryLocalisation>

  /**
   * Retrieves all categoryLocalisations from the CategoryLocalisationStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<{ active: string | null; items: CategoryLocalisation[] }>} A promise that resolves with an array of categoryLocalisations.
   */
  retrieveAll: (categoryId: string, force?: boolean) => Promise<{
    active: string | null
    items: CategoryLocalisation[]
  }>

  /**
   * Retrieves categoryLocalisations paginated from the external source.
   * @param pagination The pagination to be used for the query.
   * @param force Whether to force the query to the external source.
   * @returns {Promise<{ pagination: Pagination; items: CategoryLocalisation[] }>} A promise that resolves with paginated categoryLocalisations.
   */
  retrievePaginated: (categoryId: string, pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: CategoryLocalisation[]
  }>

  /**
   * Searches for categoryLocalisations in the external source.
   * @param search The search query string.
   * @param pagination The pagination to be used for the search.
   * @param force Whether to force the search to the external source.
   * @returns {Promise<{ pagination: Pagination; items: CategoryLocalisation[] }>} A promise that resolves with search results.
   */
  search: (categoryId: string, search: string, pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: CategoryLocalisation[]
  }>

  /**
   * Update an categoryLocalisation from the CategoryLocalisationStore in the external database.
   * @param categoryLocalisation The categoryLocalisation to be updated.
   * @returns {Promise<CategoryLocalisation>} A promise that resolves with the updated categoryLocalisation.
   */
  update: (categoryLocalisation: CategoryLocalisation) => Promise<CategoryLocalisation>
}
