import { type Tag } from './interface.gen'
import { type Store } from 'pinia'
import { Pagination } from "../../general/pagination"
import { Query } from "../../general/query"

/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the tag store.
 */
export interface TagStoreState {
  /**
   * The currently active tag ID
   */
  activeID: string | null

  /**
   * A cache of tags.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of tags.
   */
  list: Tag[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an TagStore that extends the Store interface.
 */
export interface TagStore extends TagStoreState, Store {
  /**
   * The currently active tag
   */
  active: Tag | null

  /**
   * Adds an tag to the local TagStore.
   * @param tag The tag to be added.
   */
  add: (tag: Tag) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Creates a new tag in the external database and updates the store.
   * @param tag The tag to be created.
   * @returns {Promise<Tag>} A promise that resolves with the created tag.
   */
  create: (tag: Tag) => Promise<Tag>

  /**
   * Deletes an tag from the TagStore from the external database.
   * @param tag The tag to be deleted.
   * @returns {Promise<void>} A promise that resolves when the tag is deleted.
   */
  delete: (tag: Tag) => Promise<void>

  /**
   * Deletes an tag by ID from the TagStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the tag is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Fire a form update event for the Tag.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all Tags.
   */
  emitStoreUpdateEvent: () => void

  /**
   * Get a Tag from the local TagStore.
   * @returns { Tag | null } The tag.
   */
  get: (id: string) => Tag | null

  /**
   * Queries tags from the external source
   * @param queries The queries to be used for filtering.
   * @param pagination The pagination to be used for the query.
   * @param force Whether to force the query to the external source.
   * @returns {Promise<{ pagination: Pagination; items: Tag[] }>} A promise that resolves with queried tags.
   */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Tag[]
  }>

  /**
   * Reinserts an tag to the TagStore list.
   * @param tag The tag to be reinserted.
   * @returns {void}
   */
  reinsert: (tag: Tag) => void

  /**
   * Removes an tag from the local TagStore.
   * @param tag The tag to be removed.
   * @returns {void}
   */
  remove: ({ id }: Tag) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single Tag from either the local TagStore or external source, if forced or required.
   * @param id The ID of the Tag to retrieve.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<Tag>} A promise that resolves when the tag is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<Tag>

  /**
   * Retrieves all tags from the TagStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<{ active: string | null; items: Tag[] }>} A promise that resolves with an array of tags.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: Tag[]
  }>

  /**
   * Retrieves tags paginated from the external source.
   * @param pagination The pagination to be used for the query.
   * @param force Whether to force the query to the external source.
   * @returns {Promise<{ pagination: Pagination; items: Tag[] }>} A promise that resolves with paginated tags.
   */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Tag[]
  }>

  /**
   * Searches for tags in the external source.
   * @param search The search query string.
   * @param pagination The pagination to be used for the search.
   * @param force Whether to force the search to the external source.
   * @returns {Promise<{ pagination: Pagination; items: Tag[] }>} A promise that resolves with search results.
   */
  search: (search: string, pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Tag[]
  }>

  /**
   * Update an tag from the TagStore in the external database.
   * @param tag The tag to be updated.
   * @returns {Promise<Tag>} A promise that resolves with the updated tag.
   */
  update: (tag: Tag) => Promise<Tag>
}
