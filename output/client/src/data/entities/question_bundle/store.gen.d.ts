import { type QuestionBundle } from './interface.gen'
import { type Store } from 'pinia'
import { Pagination } from "../../general/pagination"
import { Query } from "../../general/query"

/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the questionBundle store.
 */
export interface QuestionBundleStoreState {
  /**
   * The currently active questionBundle ID
   */
  activeID: string | null

  /**
   * A cache of questionBundles.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of questionBundles.
   */
  list: QuestionBundle[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an QuestionBundleStore that extends the Store interface.
 */
export interface QuestionBundleStore extends QuestionBundleStoreState, Store {
  /**
   * The currently active questionBundle
   */
  active: QuestionBundle | null

  /**
   * Adds an questionBundle to the local QuestionBundleStore.
   * @param questionBundle The questionBundle to be added.
   */
  add: (questionBundle: QuestionBundle) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Creates a new questionBundle in the external database and updates the store.
   * @param questionBundle The questionBundle to be created.
   * @returns {Promise<QuestionBundle>} A promise that resolves with the created questionBundle.
   */
  create: (questionBundle: QuestionBundle) => Promise<QuestionBundle>

  /**
   * Deletes an questionBundle from the QuestionBundleStore from the external database.
   * @param questionBundle The questionBundle to be deleted.
   * @returns {Promise<void>} A promise that resolves when the questionBundle is deleted.
   */
  delete: (questionBundle: QuestionBundle) => Promise<void>

  /**
   * Deletes an questionBundle by ID from the QuestionBundleStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the questionBundle is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Fire a form update event for the QuestionBundle.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all QuestionBundles.
   */
  emitStoreUpdateEvent: () => void

  /**
   * Get a QuestionBundle from the local QuestionBundleStore.
   * @returns { QuestionBundle | null } The questionBundle.
   */
  get: (id: string) => QuestionBundle | null

  /**
   * Queries questionBundles from the external source
   * @param queries The queries to be used for filtering.
   * @param pagination The pagination to be used for the query.
   * @param force Whether to force the query to the external source.
   * @returns {Promise<{ pagination: Pagination; items: QuestionBundle[] }>} A promise that resolves with queried questionBundles.
   */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: QuestionBundle[]
  }>

  /**
   * Reinserts an questionBundle to the QuestionBundleStore list.
   * @param questionBundle The questionBundle to be reinserted.
   * @returns {void}
   */
  reinsert: (questionBundle: QuestionBundle) => void

  /**
   * Removes an questionBundle from the local QuestionBundleStore.
   * @param questionBundle The questionBundle to be removed.
   * @returns {void}
   */
  remove: ({ id }: QuestionBundle) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single QuestionBundle from either the local QuestionBundleStore or external source, if forced or required.
   * @param id The ID of the QuestionBundle to retrieve.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<QuestionBundle>} A promise that resolves when the questionBundle is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<QuestionBundle>

  /**
   * Retrieves all questionBundles from the QuestionBundleStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<{ active: string | null; items: QuestionBundle[] }>} A promise that resolves with an array of questionBundles.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: QuestionBundle[]
  }>

  /**
   * Retrieves questionBundles paginated from the external source.
   * @param pagination The pagination to be used for the query.
   * @param force Whether to force the query to the external source.
   * @returns {Promise<{ pagination: Pagination; items: QuestionBundle[] }>} A promise that resolves with paginated questionBundles.
   */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: QuestionBundle[]
  }>

  /**
   * Searches for questionBundles in the external source.
   * @param search The search query string.
   * @param pagination The pagination to be used for the search.
   * @param force Whether to force the search to the external source.
   * @returns {Promise<{ pagination: Pagination; items: QuestionBundle[] }>} A promise that resolves with search results.
   */
  search: (search: string, pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: QuestionBundle[]
  }>

  /**
   * Update an questionBundle from the QuestionBundleStore in the external database.
   * @param questionBundle The questionBundle to be updated.
   * @returns {Promise<QuestionBundle>} A promise that resolves with the updated questionBundle.
   */
  update: (questionBundle: QuestionBundle) => Promise<QuestionBundle>
}
