import { defineStore } from 'pinia'
import { QuestionBundleLocalisation } from './interface.gen'
import type { QuestionBundleLocalisationStore, QuestionBundleLocalisationStoreState } from './store.gen.d'
import { Pagination, paginationAndQueriesToCacheKey } from "../../general/pagination";
import { Query } from "../../general/query";
import { QuestionBundleLocalisationTransporter } from './transport.gen'

/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

export const useQuestionBundleLocalisationStore = defineStore('questionBundleLocalisationStore', {
  state: (): QuestionBundleLocalisationStoreState => ({
    activeID: null,
    cache: {},
    list: [] as QuestionBundleLocalisation[],
    retrievalTimestamp: 0,
    updateKey: 0,
    storeUpdateTimeout: null,
  }),
  actions: {
    emitFormUpdateEvent(id: string): void {
      window.dispatchEvent(new CustomEvent("form.update.question_bundle_localisation", { detail: { id } }));
    },
    emitStoreUpdateEvent(): void {
      // Clear existing timeout
      if (this.storeUpdateTimeout !== null) {
        clearTimeout(this.storeUpdateTimeout)
      }

      // Set new timeout
      this.storeUpdateTimeout = setTimeout(() => {
        window.dispatchEvent(new CustomEvent("store.update.question_bundle_localisation"));
        this.storeUpdateTimeout = null
      }, 1500)
    },
    async activate(questionBundleLocalisation: QuestionBundleLocalisation): Promise<void> {
      // Set active
      this.activeID = questionBundleLocalisation.id
    },
    add(questionBundleLocalisation: QuestionBundleLocalisation): void {
      const id = questionBundleLocalisation.id
      if (typeof id === 'string' && !this.contains(id)) {
        this.list.push(questionBundleLocalisation)

        // Set active
        if (this.active === null) {
          this.activeID = questionBundleLocalisation.id
        }
      } else {
        this.reinsert(questionBundleLocalisation)
      }
      this.updateKey += 1;
    },
    async delete(questionBundleLocalisation: QuestionBundleLocalisation): Promise<void> {
      await QuestionBundleLocalisationTransporter.Delete(questionBundleLocalisation).then(() => {
        this.remove(questionBundleLocalisation)
      }).finally(() => {
        this.emitStoreUpdateEvent()
      });
    },
    async deleteById(id: string): Promise<void> {
      const questionBundleLocalisation = (this.list as QuestionBundleLocalisation[]).find((x: QuestionBundleLocalisation) => x.id === id)
      if (questionBundleLocalisation !== undefined) {
        await this.delete(questionBundleLocalisation)
      } else {
        throw new Error('Could not find QuestionBundleLocalisation with id: ' + id)
      }
    },
    contains(id: string): boolean {
      return (this.list as QuestionBundleLocalisation[]).some((x: QuestionBundleLocalisation) => x.id === id)
    },
    async create(questionBundleId: string, questionBundleLocalisation: QuestionBundleLocalisation): Promise<QuestionBundleLocalisation> {
      // Create questionBundleLocalisation
      const item = await QuestionBundleLocalisationTransporter.Create({
        questionBundle_id: questionBundleId,
      }, questionBundleLocalisation)

      // Reinsert questionBundleLocalisation
      this.reinsert(item)

      // Activate questionBundleLocalisation
      if (this.active === null) {
        await this.activate(item)
      }
      
      this.emitStoreUpdateEvent()

      return item
    },
    get(id: string): QuestionBundleLocalisation | null {
      return (this.list as QuestionBundleLocalisation[]).find((x: QuestionBundleLocalisation) => x.id === id) || null
    },
    reinsert(questionBundleLocalisation: QuestionBundleLocalisation): void {
      const index = (this.list as QuestionBundleLocalisation[]).findIndex((x: QuestionBundleLocalisation) => x.id === questionBundleLocalisation.id)
      if (index !== -1) {
        this.list = [
          ...this.list.slice(0, index),
          questionBundleLocalisation,
          ...this.list.slice(index + 1),
        ]
      } else {
        this.add(questionBundleLocalisation)
      }
      this.updateKey += 1;
    },
    remove({ id }: QuestionBundleLocalisation): void {
      const index = (this.list as QuestionBundleLocalisation[]).findIndex((x: QuestionBundleLocalisation) => x.id === id)
      if (index !== -1) {
        this.list = [
          ...this.list.slice(0, index),
          ...this.list.slice(index + 1),
        ]
      }
      this.updateKey += 1;
    },
    async retrieve(questionBundleId: string, id: string, force = false): Promise<QuestionBundleLocalisation> {
      // Check if a refresh is required
      if (!force && !this.refreshRequired) {
        const questionBundleLocalisation = this.get(id)
        if (questionBundleLocalisation !== null) {
          return questionBundleLocalisation
        }
      }

      // Get questionBundleLocalisation
      const questionBundleLocalisation = await QuestionBundleLocalisationTransporter.Get({
        question_bundle_id: question_bundleId,
        id,
      }).then(async (entity) => {
        this.reinsert(entity)

        // Activate questionBundleLocalisation
        await this.activate(entity)

        return entity
      })

      return questionBundleLocalisation
    },
    async retrieveAll(questionBundleId: string, force = false): Promise<{
      active: string | null
      items: QuestionBundleLocalisation[]
    }> {
      // Check if a refresh is required
      if (!force && !this.refreshRequired) {
        return {
          active: this.activeID,
          items: this.list as QuestionBundleLocalisation[],
        }
      }

      // Clear list
      while (this.list.length > 0) {
        this.list.pop()
      }

      // Retrieve list
      await QuestionBundleLocalisationTransporter.GetAll({
        question_bundle_id: question_bundleId,
      }).then(async (list) => {
        list.forEach((questionBundleLocalisation) => {
          this.reinsert(questionBundleLocalisation)
        })

        // Activate questionBundleLocalisation
        if (list.length > 0) {
          if (this.activeID === null) {
            await this.activate(list[0])
          }
        }
      })

      // Set retrieval timestamp
      this.retrievalTimestamp = Date.now()

      return {
        active: this.activeID,
        items: this.list as QuestionBundleLocalisation[],
      }
    },
    async retrievePaginated(questionBundleId: string, pagination: Pagination, force = false): Promise<{
      pagination: Pagination;
      items: QuestionBundleLocalisation[];
    }> {
      // Check pagination
      let newPagination = new Pagination(pagination).deepCopy();

      // Get cachekey
      const cacheKey = pagination.toCacheKey();

      // Check if a refresh is required
      if (!force && this.cache[cacheKey]) {
        const cached = this.cache[cacheKey]
        return {
          pagination: cached.pagination.deepCopy(),
          items: cached.ids.map((id: string) => this.get(id) as QuestionBundleLocalisation),
        };
      }

      // Retrieve list
      const items = await QuestionBundleLocalisationTransporter.GetPaginated({
        question_bundle_id: question_bundleId,
      },pagination).then(async ({items, pagination }) => {
        items.forEach((questionBundleLocalisation) => {
          this.reinsert(questionBundleLocalisation);
        });

        // Set new pagination
        newPagination = pagination.deepCopy();

        return items;
      });

      // Set cache
      this.cache[cacheKey] = {
        ids: items.map((x: QuestionBundleLocalisation) => x.id),
        pagination: newPagination,
        retrievalTimestamp: Date.now(),
      };

      // Set retrieval timestamp
      this.retrievalTimestamp = Date.now();

      return {
        pagination: newPagination,
        items,
      };
    },
    async query(questionBundleId: string, pagination: Pagination, queries: Query[], force = false): Promise<{
      pagination: Pagination;
      items: QuestionBundleLocalisation[];
    }> {
      // Check pagination
      let newPagination = new Pagination(pagination).deepCopy();

      // Get cachekey
      const cacheKey = paginationAndQueriesToCacheKey(pagination, queries);

      // Check if a refresh is required
      if (!force && this.cache[cacheKey]) {
        const cached = this.cache[cacheKey]
        return {
          pagination: cached.pagination.deepCopy(),
          items: cached.ids.map((id: string) => this.get(id) as QuestionBundleLocalisation),
        };
      }

      // Retrieve list
      const items = await QuestionBundleLocalisationTransporter.Query({
        question_bundle_id: question_bundleId,
      },
        pagination,
        queries,
      ).then(async ({ items, pagination }) => {
        items.forEach((questionBundleLocalisation) => {
          this.reinsert(questionBundleLocalisation);
        });

        // Set new pagination
        newPagination = pagination.deepCopy();;

        return items
      });

      // Set cache
      this.cache[cacheKey] = {
        ids: items.map((x: QuestionBundleLocalisation) => x.id),
        pagination: newPagination.deepCopy(),
        retrievalTimestamp: Date.now(),
      };

      // Set retrieval timestamp
      this.retrievalTimestamp = Date.now();

      return {
        pagination: newPagination,
        items: items,
      };
    },
    async search(
      questionBundleId: string, 
      pagination: Pagination,
      search: string,
      force = false,
    ): Promise<{
      pagination: Pagination;
      items: QuestionBundleLocalisation[];
    }> {
      // Check pagination
      let newPagination = new Pagination(pagination).deepCopy();

      // Get cachekey
      const cacheKey = search.replaceAll(" ", "_") + '-' + pagination.toCacheKey();

      // Check if a refresh is required
      if (!force && this.cache[cacheKey]) {
        const cached = this.cache[cacheKey]
        return {
          items: cached.ids.map(
            (id: string) => this.get(id) as QuestionBundleLocalisation,
          ),
          pagination: cached.pagination.deepCopy(),
        };
      }

      // Retrieve list
      const items = await QuestionBundleLocalisationTransporter.Search({
        question_bundle_id: question_bundleId,
      },
        pagination,
        search,
      ).then(async ({ items, pagination }) => {
        items.forEach((questionBundleLocalisation) => {
          this.reinsert(questionBundleLocalisation);
        });

        // Set new pagination
        newPagination = pagination.deepCopy();;

        return items
      });

      // Set cache
      this.cache[cacheKey] = {
        ids: items.map((x: QuestionBundleLocalisation) => x.id),
        pagination: newPagination.deepCopy(),
        retrievalTimestamp: Date.now(),
      };

      // Set retrieval timestamp
      this.retrievalTimestamp = Date.now();

      return {
        items: items,
        pagination: newPagination,
      };
    },
    async update(questionBundleLocalisation: QuestionBundleLocalisation): Promise<QuestionBundleLocalisation> {
      // Update questionBundleLocalisation
      const item = await QuestionBundleLocalisationTransporter.Update(questionBundleLocalisation)

      // Reinsert questionBundleLocalisation
      this.reinsert(item)

      // Activate questionBundleLocalisation
      if (this.active === null) {
        await this.activate(item)
      }
      
      this.emitStoreUpdateEvent()

      return item
    },
  },
  getters: {
    active(): QuestionBundleLocalisation | null {
      if (this.activeID === null) {
        return null
      }

      const questionBundleLocalisation = (this.list as QuestionBundleLocalisation[]).find((x: QuestionBundleLocalisation) => x.id === this.activeID)
      if (questionBundleLocalisation === undefined) {
        return null
      }

      return questionBundleLocalisation
    },
    refreshRequired(): boolean {
      return (
        this.retrievalTimestamp === 0 ||
        this.retrievalTimestamp < Date.now() - 30 * 60000 ||
        this.list.length === 0
      )
    },
  },
})

// Export a function to create a new instance of the store
export function createQuestionBundleLocalisationStore(): QuestionBundleLocalisationStore {
  const store = useQuestionBundleLocalisationStore()
  // Run the initializeStore action when the store is created
  store.initializeStore()
  return store as unknown as QuestionBundleLocalisationStore // TODO: Fix this
}
