import { defineStore } from 'pinia'
import { ProductPackageLocalisation } from './interface.gen'
import type { ProductPackageLocalisationStore, ProductPackageLocalisationStoreState } from './store.gen.d'
import { Pagination, paginationAndQueriesToCacheKey } from "../../general/pagination";
import { Query } from "../../general/query";
import { ProductPackageLocalisationTransporter } from './transport.gen'

/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

export const useProductPackageLocalisationStore = defineStore('productPackageLocalisationStore', {
  state: (): ProductPackageLocalisationStoreState => ({
    activeID: null,
    cache: {},
    list: [] as ProductPackageLocalisation[],
    retrievalTimestamp: 0,
    updateKey: 0,
    storeUpdateTimeout: null,
  }),
  actions: {
    emitFormUpdateEvent(id: string): void {
      window.dispatchEvent(new CustomEvent("form.update.product_package_localisation", { detail: { id } }));
    },
    emitStoreUpdateEvent(): void {
      // Clear existing timeout
      if (this.storeUpdateTimeout !== null) {
        clearTimeout(this.storeUpdateTimeout)
      }

      // Set new timeout
      this.storeUpdateTimeout = setTimeout(() => {
        window.dispatchEvent(new CustomEvent("store.update.product_package_localisation"));
        this.storeUpdateTimeout = null
      }, 1500)
    },
    async activate(productPackageLocalisation: ProductPackageLocalisation): Promise<void> {
      // Set active
      this.activeID = productPackageLocalisation.id
    },
    add(productPackageLocalisation: ProductPackageLocalisation): void {
      const id = productPackageLocalisation.id
      if (typeof id === 'string' && !this.contains(id)) {
        this.list.push(productPackageLocalisation)

        // Set active
        if (this.active === null) {
          this.activeID = productPackageLocalisation.id
        }
      } else {
        this.reinsert(productPackageLocalisation)
      }
      this.updateKey += 1;
    },
    async delete(productPackageLocalisation: ProductPackageLocalisation): Promise<void> {
      await ProductPackageLocalisationTransporter.Delete(productPackageLocalisation).then(() => {
        this.remove(productPackageLocalisation)
      }).finally(() => {
        this.emitStoreUpdateEvent()
      });
    },
    async deleteById(id: string): Promise<void> {
      const productPackageLocalisation = (this.list as ProductPackageLocalisation[]).find((x: ProductPackageLocalisation) => x.id === id)
      if (productPackageLocalisation !== undefined) {
        await this.delete(productPackageLocalisation)
      } else {
        throw new Error('Could not find ProductPackageLocalisation with id: ' + id)
      }
    },
    contains(id: string): boolean {
      return (this.list as ProductPackageLocalisation[]).some((x: ProductPackageLocalisation) => x.id === id)
    },
    async create(productPackageId: string, productPackageLocalisation: ProductPackageLocalisation): Promise<ProductPackageLocalisation> {
      // Create productPackageLocalisation
      const item = await ProductPackageLocalisationTransporter.Create({
        productPackage_id: productPackageId,
      }, productPackageLocalisation)

      // Reinsert productPackageLocalisation
      this.reinsert(item)

      // Activate productPackageLocalisation
      if (this.active === null) {
        await this.activate(item)
      }
      
      this.emitStoreUpdateEvent()

      return item
    },
    get(id: string): ProductPackageLocalisation | null {
      return (this.list as ProductPackageLocalisation[]).find((x: ProductPackageLocalisation) => x.id === id) || null
    },
    reinsert(productPackageLocalisation: ProductPackageLocalisation): void {
      const index = (this.list as ProductPackageLocalisation[]).findIndex((x: ProductPackageLocalisation) => x.id === productPackageLocalisation.id)
      if (index !== -1) {
        this.list = [
          ...this.list.slice(0, index),
          productPackageLocalisation,
          ...this.list.slice(index + 1),
        ]
      } else {
        this.add(productPackageLocalisation)
      }
      this.updateKey += 1;
    },
    remove({ id }: ProductPackageLocalisation): void {
      const index = (this.list as ProductPackageLocalisation[]).findIndex((x: ProductPackageLocalisation) => x.id === id)
      if (index !== -1) {
        this.list = [
          ...this.list.slice(0, index),
          ...this.list.slice(index + 1),
        ]
      }
      this.updateKey += 1;
    },
    async retrieve(productPackageId: string, id: string, force = false): Promise<ProductPackageLocalisation> {
      // Check if a refresh is required
      if (!force && !this.refreshRequired) {
        const productPackageLocalisation = this.get(id)
        if (productPackageLocalisation !== null) {
          return productPackageLocalisation
        }
      }

      // Get productPackageLocalisation
      const productPackageLocalisation = await ProductPackageLocalisationTransporter.Get({
        product_package_id: product_packageId,
        id,
      }).then(async (entity) => {
        this.reinsert(entity)

        // Activate productPackageLocalisation
        await this.activate(entity)

        return entity
      })

      return productPackageLocalisation
    },
    async retrieveAll(productPackageId: string, force = false): Promise<{
      active: string | null
      items: ProductPackageLocalisation[]
    }> {
      // Check if a refresh is required
      if (!force && !this.refreshRequired) {
        return {
          active: this.activeID,
          items: this.list as ProductPackageLocalisation[],
        }
      }

      // Clear list
      while (this.list.length > 0) {
        this.list.pop()
      }

      // Retrieve list
      await ProductPackageLocalisationTransporter.GetAll({
        product_package_id: product_packageId,
      }).then(async (list) => {
        list.forEach((productPackageLocalisation) => {
          this.reinsert(productPackageLocalisation)
        })

        // Activate productPackageLocalisation
        if (list.length > 0) {
          if (this.activeID === null) {
            await this.activate(list[0])
          }
        }
      })

      // Set retrieval timestamp
      this.retrievalTimestamp = Date.now()

      return {
        active: this.activeID,
        items: this.list as ProductPackageLocalisation[],
      }
    },
    async retrievePaginated(productPackageId: string, pagination: Pagination, force = false): Promise<{
      pagination: Pagination;
      items: ProductPackageLocalisation[];
    }> {
      // Check pagination
      let newPagination = new Pagination(pagination).deepCopy();

      // Get cachekey
      const cacheKey = pagination.toCacheKey();

      // Check if a refresh is required
      if (!force && this.cache[cacheKey]) {
        const cached = this.cache[cacheKey]
        return {
          pagination: cached.pagination.deepCopy(),
          items: cached.ids.map((id: string) => this.get(id) as ProductPackageLocalisation),
        };
      }

      // Retrieve list
      const items = await ProductPackageLocalisationTransporter.GetPaginated({
        product_package_id: product_packageId,
      },pagination).then(async ({items, pagination }) => {
        items.forEach((productPackageLocalisation) => {
          this.reinsert(productPackageLocalisation);
        });

        // Set new pagination
        newPagination = pagination.deepCopy();

        return items;
      });

      // Set cache
      this.cache[cacheKey] = {
        ids: items.map((x: ProductPackageLocalisation) => x.id),
        pagination: newPagination,
        retrievalTimestamp: Date.now(),
      };

      // Set retrieval timestamp
      this.retrievalTimestamp = Date.now();

      return {
        pagination: newPagination,
        items,
      };
    },
    async query(productPackageId: string, pagination: Pagination, queries: Query[], force = false): Promise<{
      pagination: Pagination;
      items: ProductPackageLocalisation[];
    }> {
      // Check pagination
      let newPagination = new Pagination(pagination).deepCopy();

      // Get cachekey
      const cacheKey = paginationAndQueriesToCacheKey(pagination, queries);

      // Check if a refresh is required
      if (!force && this.cache[cacheKey]) {
        const cached = this.cache[cacheKey]
        return {
          pagination: cached.pagination.deepCopy(),
          items: cached.ids.map((id: string) => this.get(id) as ProductPackageLocalisation),
        };
      }

      // Retrieve list
      const items = await ProductPackageLocalisationTransporter.Query({
        product_package_id: product_packageId,
      },
        pagination,
        queries,
      ).then(async ({ items, pagination }) => {
        items.forEach((productPackageLocalisation) => {
          this.reinsert(productPackageLocalisation);
        });

        // Set new pagination
        newPagination = pagination.deepCopy();;

        return items
      });

      // Set cache
      this.cache[cacheKey] = {
        ids: items.map((x: ProductPackageLocalisation) => x.id),
        pagination: newPagination.deepCopy(),
        retrievalTimestamp: Date.now(),
      };

      // Set retrieval timestamp
      this.retrievalTimestamp = Date.now();

      return {
        pagination: newPagination,
        items: items,
      };
    },
    async search(
      productPackageId: string, 
      pagination: Pagination,
      search: string,
      force = false,
    ): Promise<{
      pagination: Pagination;
      items: ProductPackageLocalisation[];
    }> {
      // Check pagination
      let newPagination = new Pagination(pagination).deepCopy();

      // Get cachekey
      const cacheKey = search.replaceAll(" ", "_") + '-' + pagination.toCacheKey();

      // Check if a refresh is required
      if (!force && this.cache[cacheKey]) {
        const cached = this.cache[cacheKey]
        return {
          items: cached.ids.map(
            (id: string) => this.get(id) as ProductPackageLocalisation,
          ),
          pagination: cached.pagination.deepCopy(),
        };
      }

      // Retrieve list
      const items = await ProductPackageLocalisationTransporter.Search({
        product_package_id: product_packageId,
      },
        pagination,
        search,
      ).then(async ({ items, pagination }) => {
        items.forEach((productPackageLocalisation) => {
          this.reinsert(productPackageLocalisation);
        });

        // Set new pagination
        newPagination = pagination.deepCopy();;

        return items
      });

      // Set cache
      this.cache[cacheKey] = {
        ids: items.map((x: ProductPackageLocalisation) => x.id),
        pagination: newPagination.deepCopy(),
        retrievalTimestamp: Date.now(),
      };

      // Set retrieval timestamp
      this.retrievalTimestamp = Date.now();

      return {
        items: items,
        pagination: newPagination,
      };
    },
    async update(productPackageLocalisation: ProductPackageLocalisation): Promise<ProductPackageLocalisation> {
      // Update productPackageLocalisation
      const item = await ProductPackageLocalisationTransporter.Update(productPackageLocalisation)

      // Reinsert productPackageLocalisation
      this.reinsert(item)

      // Activate productPackageLocalisation
      if (this.active === null) {
        await this.activate(item)
      }
      
      this.emitStoreUpdateEvent()

      return item
    },
  },
  getters: {
    active(): ProductPackageLocalisation | null {
      if (this.activeID === null) {
        return null
      }

      const productPackageLocalisation = (this.list as ProductPackageLocalisation[]).find((x: ProductPackageLocalisation) => x.id === this.activeID)
      if (productPackageLocalisation === undefined) {
        return null
      }

      return productPackageLocalisation
    },
    refreshRequired(): boolean {
      return (
        this.retrievalTimestamp === 0 ||
        this.retrievalTimestamp < Date.now() - 30 * 60000 ||
        this.list.length === 0
      )
    },
  },
})

// Export a function to create a new instance of the store
export function createProductPackageLocalisationStore(): ProductPackageLocalisationStore {
  const store = useProductPackageLocalisationStore()
  // Run the initializeStore action when the store is created
  store.initializeStore()
  return store as unknown as ProductPackageLocalisationStore // TODO: Fix this
}
