import { type Media } from './interface.gen'
import { type Store } from 'pinia'
import { Pagination } from "../../general/pagination"
import { Query } from "../../general/query"

/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the media store.
 */
export interface MediaStoreState {
  /**
   * The currently active media ID
   */
  activeID: string | null

  /**
   * A cache of medias.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of medias.
   */
  list: Media[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an MediaStore that extends the Store interface.
 */
export interface MediaStore extends MediaStoreState, Store {
  /**
   * The currently active media
   */
  active: Media | null

  /**
   * Adds an media to the local MediaStore.
   * @param media The media to be added.
   */
  add: (media: Media) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Creates a new media in the external database and updates the store.
   * @param media The media to be created.
   * @returns {Promise<Media>} A promise that resolves with the created media.
   */
  create: (media: Media) => Promise<Media>

  /**
   * Deletes an media from the MediaStore from the external database.
   * @param media The media to be deleted.
   * @returns {Promise<void>} A promise that resolves when the media is deleted.
   */
  delete: (media: Media) => Promise<void>

  /**
   * Deletes an media by ID from the MediaStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the media is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Fire a form update event for the Media.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all Medias.
   */
  emitStoreUpdateEvent: () => void

  /**
   * Get a Media from the local MediaStore.
   * @returns { Media | null } The media.
   */
  get: (id: string) => Media | null

  /**
   * Queries medias from the external source
   * @param queries The queries to be used for filtering.
   * @param pagination The pagination to be used for the query.
   * @param force Whether to force the query to the external source.
   * @returns {Promise<{ pagination: Pagination; items: Media[] }>} A promise that resolves with queried medias.
   */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Media[]
  }>

  /**
   * Reinserts an media to the MediaStore list.
   * @param media The media to be reinserted.
   * @returns {void}
   */
  reinsert: (media: Media) => void

  /**
   * Removes an media from the local MediaStore.
   * @param media The media to be removed.
   * @returns {void}
   */
  remove: ({ id }: Media) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single Media from either the local MediaStore or external source, if forced or required.
   * @param id The ID of the Media to retrieve.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<Media>} A promise that resolves when the media is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<Media>

  /**
   * Retrieves all medias from the MediaStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<{ active: string | null; items: Media[] }>} A promise that resolves with an array of medias.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: Media[]
  }>

  /**
   * Retrieves medias paginated from the external source.
   * @param pagination The pagination to be used for the query.
   * @param force Whether to force the query to the external source.
   * @returns {Promise<{ pagination: Pagination; items: Media[] }>} A promise that resolves with paginated medias.
   */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Media[]
  }>

  /**
   * Searches for medias in the external source.
   * @param search The search query string.
   * @param pagination The pagination to be used for the search.
   * @param force Whether to force the search to the external source.
   * @returns {Promise<{ pagination: Pagination; items: Media[] }>} A promise that resolves with search results.
   */
  search: (search: string, pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Media[]
  }>

  /**
   * Update an media from the MediaStore in the external database.
   * @param media The media to be updated.
   * @returns {Promise<Media>} A promise that resolves with the updated media.
   */
  update: (media: Media) => Promise<Media>
}
