import { type Product } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the product store.
 */
export interface ProductStoreState {
  /**
   * The currently active product ID
   */
  activeID: string | null

  /**
   * A cache of products.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of products.
   */
  list: Product[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an ProductStore that extends the Store interface.
 */
export interface ProductStore extends ProductStoreState, Store {
  /**
   * Fire a form update event for the Product.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all Product.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active product
   */
  active: Product

  /**
   * Adds an product to the local ProductStore.
   * @param product The product to be added.
   */
  add: (product: Product) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an product from the ProductStore from the external database.
   * @param product The product to be deleted.
   * @returns {Promise<void>} A promise that resolves when the product is deleted.
   */
  delete: (product: Product) => Promise<void>

  /**
   * Deletes an product by ID from the ProductStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the product is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a Product from the local ProductStore.
   * @returns { Product | null } The product.
   */
  get: (id: string) => Product | null

  /**
   * Reinserts an product to the ProductStore list.
   * @param product The product to be reinserted.
   * @returns {void}
   */
  reinsert: (product: Product) => void

  /**
   * Removes an product from the local ProductStore.
   * @param product The product to be removed.
   * @returns {void}
   */
  remove: (product: Product) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single Product from either the local ProductStore or external source, if forced or required.
    * @param id The ID of the Product to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the product is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<Product>

  /**
   * Retrieves all products from the ProductStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of products.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: Product[]
  }>

  /**
    * Retrieves products paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of products.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Product[]
  }>

  /**
    * Queries products from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of products.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Product[]
  }>

  /**
   * Update an product from the ProductStore in the external database.
   * @param product The product to be updated.
   * @param connect Indicates whether the product should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the product is updated.
   */
  update: (product: Product) => Promise<string>

  /**
    * Searches for products in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of products.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<Product[]>
}
