import { type AttributeOption } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the attributeOption store.
 */
export interface AttributeOptionStoreState {
  /**
   * The currently active attributeOption ID
   */
  activeID: string | null

  /**
   * A cache of attributeOptions.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of attributeOptions.
   */
  list: AttributeOption[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an AttributeOptionStore that extends the Store interface.
 */
export interface AttributeOptionStore extends AttributeOptionStoreState, Store {
  /**
   * Fire a form update event for the AttributeOption.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all AttributeOption.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active attributeOption
   */
  active: AttributeOption

  /**
   * Adds an attributeOption to the local AttributeOptionStore.
   * @param attributeOption The attributeOption to be added.
   */
  add: (attributeOption: AttributeOption) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an attributeOption from the AttributeOptionStore from the external database.
   * @param attributeOption The attributeOption to be deleted.
   * @returns {Promise<void>} A promise that resolves when the attributeOption is deleted.
   */
  delete: (attributeOption: AttributeOption) => Promise<void>

  /**
   * Deletes an attributeOption by ID from the AttributeOptionStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the attributeOption is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a AttributeOption from the local AttributeOptionStore.
   * @returns { AttributeOption | null } The attributeOption.
   */
  get: (id: string) => AttributeOption | null

  /**
   * Reinserts an attributeOption to the AttributeOptionStore list.
   * @param attributeOption The attributeOption to be reinserted.
   * @returns {void}
   */
  reinsert: (attributeOption: AttributeOption) => void

  /**
   * Removes an attributeOption from the local AttributeOptionStore.
   * @param attributeOption The attributeOption to be removed.
   * @returns {void}
   */
  remove: (attributeOption: AttributeOption) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single AttributeOption from either the local AttributeOptionStore or external source, if forced or required.
    * @param id The ID of the AttributeOption to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the attributeOption is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<AttributeOption>

  /**
   * Retrieves all attributeOptions from the AttributeOptionStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of attributeOptions.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: AttributeOption[]
  }>

  /**
    * Retrieves attributeOptions paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of attributeOptions.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: AttributeOption[]
  }>

  /**
    * Queries attributeOptions from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of attributeOptions.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: AttributeOption[]
  }>

  /**
   * Update an attributeOption from the AttributeOptionStore in the external database.
   * @param attributeOption The attributeOption to be updated.
   * @param connect Indicates whether the attributeOption should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the attributeOption is updated.
   */
  update: (attributeOption: AttributeOption) => Promise<string>

  /**
    * Searches for attributeOptions in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of attributeOptions.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<AttributeOption[]>
}
