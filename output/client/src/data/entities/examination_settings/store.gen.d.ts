import { type ExaminationSettings } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the examinationSettings store.
 */
export interface ExaminationSettingsStoreState {
  /**
   * The currently active examinationSettings ID
   */
  activeID: string | null

  /**
   * A cache of examinationSettingss.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of examinationSettingss.
   */
  list: ExaminationSettings[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an ExaminationSettingsStore that extends the Store interface.
 */
export interface ExaminationSettingsStore extends ExaminationSettingsStoreState, Store {
  /**
   * Fire a form update event for the ExaminationSettings.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all ExaminationSettings.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active examinationSettings
   */
  active: ExaminationSettings

  /**
   * Adds an examinationSettings to the local ExaminationSettingsStore.
   * @param examinationSettings The examinationSettings to be added.
   */
  add: (examinationSettings: ExaminationSettings) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an examinationSettings from the ExaminationSettingsStore from the external database.
   * @param examinationSettings The examinationSettings to be deleted.
   * @returns {Promise<void>} A promise that resolves when the examinationSettings is deleted.
   */
  delete: (examinationSettings: ExaminationSettings) => Promise<void>

  /**
   * Deletes an examinationSettings by ID from the ExaminationSettingsStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the examinationSettings is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a ExaminationSettings from the local ExaminationSettingsStore.
   * @returns { ExaminationSettings | null } The examinationSettings.
   */
  get: (id: string) => ExaminationSettings | null

  /**
   * Reinserts an examinationSettings to the ExaminationSettingsStore list.
   * @param examinationSettings The examinationSettings to be reinserted.
   * @returns {void}
   */
  reinsert: (examinationSettings: ExaminationSettings) => void

  /**
   * Removes an examinationSettings from the local ExaminationSettingsStore.
   * @param examinationSettings The examinationSettings to be removed.
   * @returns {void}
   */
  remove: (examinationSettings: ExaminationSettings) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single ExaminationSettings from either the local ExaminationSettingsStore or external source, if forced or required.
    * @param id The ID of the ExaminationSettings to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the examinationSettings is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<ExaminationSettings>

  /**
   * Retrieves all examinationSettingss from the ExaminationSettingsStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of examinationSettingss.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: ExaminationSettings[]
  }>

  /**
    * Retrieves examinationSettingss paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of examinationSettingss.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: ExaminationSettings[]
  }>

  /**
    * Queries examinationSettingss from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of examinationSettingss.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: ExaminationSettings[]
  }>

  /**
   * Update an examinationSettings from the ExaminationSettingsStore in the external database.
   * @param examinationSettings The examinationSettings to be updated.
   * @param connect Indicates whether the examinationSettings should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the examinationSettings is updated.
   */
  update: (examinationSettings: ExaminationSettings) => Promise<string>

  /**
    * Searches for examinationSettingss in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of examinationSettingss.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<ExaminationSettings[]>
}
