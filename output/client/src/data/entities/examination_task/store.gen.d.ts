import { type ExaminationTask } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the examinationTask store.
 */
export interface ExaminationTaskStoreState {
  /**
   * The currently active examinationTask ID
   */
  activeID: string | null

  /**
   * A cache of examinationTasks.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of examinationTasks.
   */
  list: ExaminationTask[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an ExaminationTaskStore that extends the Store interface.
 */
export interface ExaminationTaskStore extends ExaminationTaskStoreState, Store {
  /**
   * Fire a form update event for the ExaminationTask.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all ExaminationTask.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active examinationTask
   */
  active: ExaminationTask

  /**
   * Adds an examinationTask to the local ExaminationTaskStore.
   * @param examinationTask The examinationTask to be added.
   */
  add: (examinationTask: ExaminationTask) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an examinationTask from the ExaminationTaskStore from the external database.
   * @param examinationTask The examinationTask to be deleted.
   * @returns {Promise<void>} A promise that resolves when the examinationTask is deleted.
   */
  delete: (examinationTask: ExaminationTask) => Promise<void>

  /**
   * Deletes an examinationTask by ID from the ExaminationTaskStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the examinationTask is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a ExaminationTask from the local ExaminationTaskStore.
   * @returns { ExaminationTask | null } The examinationTask.
   */
  get: (id: string) => ExaminationTask | null

  /**
   * Reinserts an examinationTask to the ExaminationTaskStore list.
   * @param examinationTask The examinationTask to be reinserted.
   * @returns {void}
   */
  reinsert: (examinationTask: ExaminationTask) => void

  /**
   * Removes an examinationTask from the local ExaminationTaskStore.
   * @param examinationTask The examinationTask to be removed.
   * @returns {void}
   */
  remove: (examinationTask: ExaminationTask) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single ExaminationTask from either the local ExaminationTaskStore or external source, if forced or required.
    * @param id The ID of the ExaminationTask to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the examinationTask is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<ExaminationTask>

  /**
   * Retrieves all examinationTasks from the ExaminationTaskStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of examinationTasks.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: ExaminationTask[]
  }>

  /**
    * Retrieves examinationTasks paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of examinationTasks.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: ExaminationTask[]
  }>

  /**
    * Queries examinationTasks from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of examinationTasks.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: ExaminationTask[]
  }>

  /**
   * Update an examinationTask from the ExaminationTaskStore in the external database.
   * @param examinationTask The examinationTask to be updated.
   * @param connect Indicates whether the examinationTask should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the examinationTask is updated.
   */
  update: (examinationTask: ExaminationTask) => Promise<string>

  /**
    * Searches for examinationTasks in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of examinationTasks.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<ExaminationTask[]>
}
