import { type PriceConfiguration } from './entity'
import { type Store } from 'pinia'


/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the priceConfiguration store.
 */
export interface PriceConfigurationStoreState {
  /**
   * The currently active priceConfiguration ID
   */
  activeID: string | null

  /**
   * A cache of priceConfigurations.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of priceConfigurations.
   */
  list: PriceConfiguration[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an PriceConfigurationStore that extends the Store interface.
 */
export interface PriceConfigurationStore extends PriceConfigurationStoreState, Store {
  /**
   * Fire a form update event for the PriceConfiguration.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all PriceConfiguration.
   */
  emitStoreUpdateEvent: () => void

  /**
   * The currently active priceConfiguration
   */
  active: PriceConfiguration

  /**
   * Adds an priceConfiguration to the local PriceConfigurationStore.
   * @param priceConfiguration The priceConfiguration to be added.
   */
  add: (priceConfiguration: PriceConfiguration) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Deletes an priceConfiguration from the PriceConfigurationStore from the external database.
   * @param priceConfiguration The priceConfiguration to be deleted.
   * @returns {Promise<void>} A promise that resolves when the priceConfiguration is deleted.
   */
  delete: (priceConfiguration: PriceConfiguration) => Promise<void>

  /**
   * Deletes an priceConfiguration by ID from the PriceConfigurationStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the priceConfiguration is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Get a PriceConfiguration from the local PriceConfigurationStore.
   * @returns { PriceConfiguration | null } The priceConfiguration.
   */
  get: (id: string) => PriceConfiguration | null

  /**
   * Reinserts an priceConfiguration to the PriceConfigurationStore list.
   * @param priceConfiguration The priceConfiguration to be reinserted.
   * @returns {void}
   */
  reinsert: (priceConfiguration: PriceConfiguration) => void

  /**
   * Removes an priceConfiguration from the local PriceConfigurationStore.
   * @param priceConfiguration The priceConfiguration to be removed.
   * @returns {void}
   */
  remove: (priceConfiguration: PriceConfiguration) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single PriceConfiguration from either the local PriceConfigurationStore or external source, if forced or required.
    * @param id The ID of the PriceConfiguration to retrieve.
    * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves when the priceConfiguration is set as active.
   */
  retrieve: (id: string, force?: boolean) => Promise<PriceConfiguration>

  /**
   * Retrieves all priceConfigurations from the PriceConfigurationStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<void>} A promise that resolves with an array of priceConfigurations.
   */
  retrieveAll: (force?: boolean) => Promise<{
    active: string | null
    items: PriceConfiguration[]
  }>

  /**
    * Retrieves priceConfigurations paginated from the external source
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of priceConfigurations.
    */
  retrievePaginated: (pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: PriceConfiguration[]
  }>

  /**
    * Queries priceConfigurations from the external source
    * @param queries The queries to be used for filtering.
    * @param pagination The pagination to be used for the query.
    * @param force Whether to force the query to the external source.
    * @returns {Promise<void>} A promise that resolves with an array of priceConfigurations.
    */
  query: (queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: PriceConfiguration[]
  }>

  /**
   * Update an priceConfiguration from the PriceConfigurationStore in the external database.
   * @param priceConfiguration The priceConfiguration to be updated.
   * @param connect Indicates whether the priceConfiguration should be connected to the user on update.
   * @returns {Promise<string>} A promise that resolves to the ID as a string when the priceConfiguration is updated.
   */
  update: (priceConfiguration: PriceConfiguration) => Promise<string>

  /**
    * Searches for priceConfigurations in the external source.
    * @param query The query to be used for searching.
    * @returns {Promise<void>} A promise that resolves with an array of priceConfigurations.
    */
  search: (query: string, pagination: Pagination, force?: boolean) => Promise<PriceConfiguration[]>
}
