import { type Purchase } from './interface.gen'
import { type Store } from 'pinia'
import { Pagination } from "../../general/pagination"
import { Query } from "../../general/query"

/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// IF YOU WANT TO EDIT THIS FILE, REMOVE THE .gen. FROM THE FILENAME. THE FILE WILL THEN BE IGNORED.
/// ///////////////////////////////////////////////////////////////////////////////////////////////////

/**
 * Represents the state of the purchase store.
 */
export interface PurchaseStoreState {
  /**
   * The currently active purchase ID
   */
  activeID: string | null

  /**
   * A cache of purchases.
   */
  cache: Record<string, {
    ids: string[]
    pagination: Pagination
    retrievalTimestamp: number
  }>

  /**
   * The list of purchases.
   */
  list: Purchase[]

  /**
   * The retrieval timestamp
   */
  retrievalTimestamp: number

  /**
   * A key that is updated to force the update of the entity.
   */
  updateKey: number

  /**
   * A timeout before emitting a store update event.
   */
  storeUpdateTimeout: window.Timeout | null
}

/**
 * Represents an PurchaseStore that extends the Store interface.
 */
export interface PurchaseStore extends PurchaseStoreState, Store {
  /**
   * The currently active purchase
   */
  active: Purchase | null

  /**
   * Adds an purchase to the local PurchaseStore.
   * @param purchase The purchase to be added.
   */
  add: (purchase: Purchase) => void

  /**
   * Checks whether the list contains an instance with the provided ID.
   * @param id The id to be searched.
   * @returns {boolean} A boolean indicating whether the list contains the ID.
   */
  contains: (id: string) => boolean

  /**
   * Creates a new purchase in the external database and updates the store.
   * @param purchase The purchase to be created.
   * @returns {Promise<Purchase>} A promise that resolves with the created purchase.
   */
  create: (userId: string, purchase: Purchase) => Promise<Purchase>

  /**
   * Deletes an purchase from the PurchaseStore from the external database.
   * @param purchase The purchase to be deleted.
   * @returns {Promise<void>} A promise that resolves when the purchase is deleted.
   */
  delete: (purchase: Purchase) => Promise<void>

  /**
   * Deletes an purchase by ID from the PurchaseStore from the external database.
   * @param id The ID to be deleted.
   * @returns {Promise<void>} A promise that resolves when the purchase is deleted.
   */
  deleteById: (id: string) => Promise<void>

  /**
   * Fire a form update event for the Purchase.
   */
  emitFormUpdateEvent: (id: string) => void

  /**
   * Fire a store update event for all Purchases.
   */
  emitStoreUpdateEvent: () => void

  /**
   * Get a Purchase from the local PurchaseStore.
   * @returns { Purchase | null } The purchase.
   */
  get: (id: string) => Purchase | null

  /**
   * Queries purchases from the external source
   * @param queries The queries to be used for filtering.
   * @param pagination The pagination to be used for the query.
   * @param force Whether to force the query to the external source.
   * @returns {Promise<{ pagination: Pagination; items: Purchase[] }>} A promise that resolves with queried purchases.
   */
  query: (userId: string, queries: Query[], pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Purchase[]
  }>

  /**
   * Reinserts an purchase to the PurchaseStore list.
   * @param purchase The purchase to be reinserted.
   * @returns {void}
   */
  reinsert: (purchase: Purchase) => void

  /**
   * Removes an purchase from the local PurchaseStore.
   * @param purchase The purchase to be removed.
   * @returns {void}
   */
  remove: ({ id }: Purchase) => void

  /**
   * Determines whether a refresh of the store is required
   */
  refreshRequired: boolean

  /**
   * Retrieves a single Purchase from either the local PurchaseStore or external source, if forced or required.
   * @param id The ID of the Purchase to retrieve.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<Purchase>} A promise that resolves when the purchase is set as active.
   */
  retrieve: (userId: string, id: string, force?: boolean) => Promise<Purchase>

  /**
   * Retrieves all purchases from the PurchaseStore or external source, depending if a refresh is required.
   * @param force Indicates whether a refresh is forced.
   * @returns {Promise<{ active: string | null; items: Purchase[] }>} A promise that resolves with an array of purchases.
   */
  retrieveAll: (userId: string, force?: boolean) => Promise<{
    active: string | null
    items: Purchase[]
  }>

  /**
   * Retrieves purchases paginated from the external source.
   * @param pagination The pagination to be used for the query.
   * @param force Whether to force the query to the external source.
   * @returns {Promise<{ pagination: Pagination; items: Purchase[] }>} A promise that resolves with paginated purchases.
   */
  retrievePaginated: (userId: string, pagination?: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Purchase[]
  }>

  /**
   * Searches for purchases in the external source.
   * @param search The search query string.
   * @param pagination The pagination to be used for the search.
   * @param force Whether to force the search to the external source.
   * @returns {Promise<{ pagination: Pagination; items: Purchase[] }>} A promise that resolves with search results.
   */
  search: (userId: string, search: string, pagination: Pagination, force?: boolean) => Promise<{
    pagination: Pagination
    items: Purchase[]
  }>

  /**
   * Update an purchase from the PurchaseStore in the external database.
   * @param purchase The purchase to be updated.
   * @returns {Promise<Purchase>} A promise that resolves with the updated purchase.
   */
  update: (purchase: Purchase) => Promise<Purchase>
}
