import { type JSONData } from '../../base_classes/dto.d'
import type IDto from '../../base_classes/dto.d'

/// ///////////////////////////////////////////////////////////////////////////////////////////////////
/// WARNING! THIS FILE IS AUTOMATICALLY GENERATED, ANY CHANGES WILL BE LOST ON NEXT GENERATION
/// ///////////////////////////////////////////////////////////////////////////////////////////////////


// Types
export type Permission = "none" | "read" | "read_write"
export type Language = "en" | "nl"

// Interfaces
export interface IAccessLevel {
    // The user's administrative access level
      admin: Permission
    // The user's access level to the airlines tool
      airlines: Permission
    // The user's access level to the API documentation tool
      apiDocs: Permission
    // The user's access level to the packing slip tool
      packingSlips: Permission
    // The user's access level to the price configuration tool
      priceConfiguration: Permission
    // The user's access level to the printing orders tool
      printingOrders: Permission
    // The user's access level to the quotation tool
      quotation: Permission
    // The user's access level to the sample test tool
      sampleTest: Permission
    // The user's access level to the user management tool
      userManagement: Permission
}

// Data Transfer Object Interface
export interface IUserProfileDto extends IDto {
  /// ///////////////////////////////////////
  /// //////// PRIVATE VARIABLES ////////////
  /// ///////////////////////////////////////
  /**
   * The path to the corresponding API endpoint with variables marked as {?}
   */
  _path: string

  /// ///////////////////////////////////////
  /// ///////// PUBLIC VARIABLES ////////////
  /// ///////////////////////////////////////

    // Which tools the user has access to
    accessLevel: IAccessLevel

    // A user's address
    address: Record<string, any>

    // A user's avatar
    avatar: media.Media

    // The URL of a user's avatar
    avatarUrl: string

    // A user's email address
    email: string

    // A user's first name
    firstName: string

    // A user's infix
    infix: string

    // Whether the user has administrative rights
    isAdmin: boolean

    // The kind of the object
    _kind: string

    // A user's language
    language: Language

    // A user's last name
    lastName: string

    // A user's prefix
    prefix: string

  /// ///////////////////////////////////////
  /// ///////// PUBLIC VARIABLES ////////////
  /// ///////////////////////////////////////

  /**
   * Set the UserProfile's properties
   */
  set: (data: JSONData) => void
}
