package cloud_function

var ExampleJSON = `{
    description: string, // The cloud function's description
    _kind: string, // The kind of the object
    name: string, // The cloud function's name
    region: string, // The cloud function's region
    runtime: string, // The cloud function's runtime
    triggerType: string, // The cloud function's trigger type
    triggerValue: string, // The cloud function's trigger value
}`