package category_localisation

var ExampleJSON = `{
    category_id: string, // The ID of the parent entity for which this is the child document
    label: string, // The label of the category
    language_id: string, // The default language code for the category
}`