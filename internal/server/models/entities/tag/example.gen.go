package tag

var ExampleJSON = `{
    label: string, // The label of the tag
    language_id: string, // The default language code for the tag
    language_ids: string[], // A list of language id's in which the question bundle is available
}`