package tag_localisation

var ExampleJSON = `{
    label: string, // The label of the tag
    language_id: string, // The default language code for the tag
    tag_id: string, // The ID of the parent entity for which this is the child document
}`