package product_package_localisation_routes

import (
	"github.com/gin-gonic/gin"
)

func InitCustomRoutes(r *gin.RouterGroup) {
	// Create custom routes for the product_package_localisation entity
	// group := r.Group("/product_package/:productPackageID/product_package_localisation")

	// Add authentication middleware for all endpoints
	// group.Use(middleware.AuthenticatedRoutes())
}
