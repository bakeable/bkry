package game_modus_localisation_routes

import (
	"github.com/gin-gonic/gin"
)

func InitCustomRoutes(r *gin.RouterGroup) {
	// Create custom routes for the game_modus_localisation entity
	// group := r.Group("/game_modus/:gameModusID/game_modus_localisation")

	// Add authentication middleware for all endpoints
	// group.Use(middleware.AuthenticatedRoutes())
}
