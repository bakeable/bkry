package question_bundle_localisation_routes

import (
	"github.com/gin-gonic/gin"
)

func InitCustomRoutes(r *gin.RouterGroup) {
	// Create custom routes for the question_bundle_localisation entity
	// group := r.Group("/question_bundle/:questionBundleID/question_bundle_localisation")

	// Add authentication middleware for all endpoints
	// group.Use(middleware.AuthenticatedRoutes())
}
