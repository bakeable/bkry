package question_context_localisation_routes

import (
	"github.com/gin-gonic/gin"
)

func InitCustomRoutes(r *gin.RouterGroup) {
	// Create custom routes for the question_context_localisation entity
	// group := r.Group("/question_context/:questionContextID/question_context_localisation")

	// Add authentication middleware for all endpoints
	// group.Use(middleware.AuthenticatedRoutes())
}
